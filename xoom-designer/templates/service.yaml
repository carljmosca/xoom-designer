apiVersion: v1
kind: Service
metadata:
  name: {{ include "xoom-designer.fullname" . }}
  labels:
    {{- include "xoom-designer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xoom-designer.selectorLabels" . | nindent 4 }}
