apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xoom-designer.fullname" . }}-test-connection"
  labels:
    {{- include "xoom-designer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xoom-designer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
